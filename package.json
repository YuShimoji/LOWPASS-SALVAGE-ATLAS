{
  "name": "lowpass-salvage-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "build:external": "npm run typecheck && vite build --mode external && node scripts/verify-external-asset-boundary.mjs",
    "evidence:phase-g": "node scripts/capture-phase-g-browser-evidence.mjs",
    "preview": "vite preview",
    "assets:import-lowpass-canary": "node scripts/import-lowpass-canary.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
